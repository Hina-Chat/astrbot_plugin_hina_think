{
    "display_reasoning_text": {
        "description": "顯示人物思維",
        "type": "bool",
        "default": true
    },
    "max_think_length": {
        "description": "使用 /think 時輸出的最大長度",
        "type": "int",
        "default": 600
    },
    "enable_persistence": {
        "description": "是否啟用持久化儲存（保存思維鏈到檔案）",
        "type": "bool",
        "default": true
    },
    "save_as_markdown": {
        "description": "是否保存為Markdown格式（便於閱讀）",
        "type": "bool",
        "default": true
    },
    "max_file_size_mb": {
        "description": "單個檔案最大大小限制（MB）",
        "type": "int",
        "default": 3
    },
    "records_per_file": {
        "description": "每個檔案最大記錄數",
        "type": "int",
        "default": 15
    },
    "max_records_per_user": {
        "description": "每用戶最大保留檔案數（超過會刪除舊檔案）",
        "type": "int",
        "default": 30
    },
    "storage_dir": {
        "description": "儲存目錄路徑（留空使用預設插件目錄）",
        "type": "text",
        "default": ""
    }
}